# EB should ignore most build artifacts but include the final JAR
build/

# Exclude large cached directories (15GB+ bandwidth waste)
.elasticbeanstalk/app_versions/
.elasticbeanstalk/logs/
.git/
.gradle/

# Exclude documentation and development files
*.md
docs/
bugs/
Features/
scripts/
util/
.claude/
.idea/
.vscode/
.DS_Store

# Exclude test and development artifacts
src/test/
*.log
*.diff
*.csv
*.svg
*.json
*.p8
# Exclude root-level shell scripts (but keep .platform/hooks/*.sh)
# NOTE: .ebignore doesn't support ! negation patterns like .gitignore
# So we exclude specific scripts instead of *.sh
deploy*.sh
run*.sh
setup*.sh
build*.sh
test*.sh

# Keep only essential deployment files
# application.jar (copied from build/libs/)
# Procfile
# .ebextensions/ (if exists)
# .platform/ (if exists)
